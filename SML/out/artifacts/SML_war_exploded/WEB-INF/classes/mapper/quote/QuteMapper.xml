<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sml.quote.QuoteMapper">

    <!-- 테스트용 -->
    <select id="selectQuoteKRWList" resultType="LinkedHashMap">
        SELECT
            MARKET
            , KOR_NM
            , ENG_NM
        FROM T_COIN_QUOTE_KRW
    </select>

    <select id="selectThKRWUpdDt" resultType="String">
        SELECT
            UPD_DT
        FROM T_COIN_QUOTE_KRW
        WHERE MARKET = 'KRW-BTC'
    </select>

    <!-- 08:55분 경주마달리기 전 현재 코인리스트 ( 밑에 INSERT 컬럼 더 추가하기 ) -->
    <insert id="insertBeforeRaceList" parameterType="LinkedHashMap">
        INSERT INTO T_COIN_RACE_KRW
        (MARKET, KOR_NM, ENG_NM, TRADE_TIME, UPD_DT)
        SELECT MARKET, KOR_NM, ENG_NM, TRADE_TIME, UPD_DT FROM T_COIN_QUOTE_KRW
    </insert>

    <!-- AM 10:00 코인 리스트 (QuoteMapper로 옮기기 (  QuoteMapper 이름도 바꾸기 (10시간을 명확하게)) -->
    <insert id="insertCoin10KRW" parameterType="LinkedHashMap">
        INSERT INTO T_COIN_10_KRW
        (
            MARKET
            , TRADE_DATE
            , TRADE_TIME
            , TRADE_DATE_KST
            , TRADE_TIME_KST
            , TRADE_TIMESTAMP
            , OPENING_PRICE
            , HIGH_PRICE
            , LOW_PRICE
            , TRADE_PRICE
            , PREV_CLOSING_PRICE
            , CHANGE
            , CHANGE_PRICE
            , CHANGE_RATE
            , SIGNED_CHANGE_PRICE
            , SIGNED_CHANGE_RATE
            , TRADE_VOLUME
            , ACC_TRADE_PRICE
            , ACC_TRADE_PRICE_24H
            , ACC_TRADE_VOLUME
            , ACC_TRADE_VOLUME_24H
            , HIGHEST_52_WEEK_PRICE
            , HIGHEST_52_WEEK_DATE
            , LOWEST_52_WEEK_PRICE
            , LOWEST_52_WEEK_DATE
            , UPD_DT
        )
        <foreach item="item" collection="list" separator="union">
            SELECT
                #{item.MARKET}
                , #{item.TRADE_DATE}
                , #{item.TRADE_TIME}
                , #{item.TRADE_DATE_KST}
                , #{item.TRADE_TIME_KST}
                , #{item.TRADE_TIMESTAMP}
                , #{item.OPENING_PRICE}
                , #{item.HIGH_PRICE}
                , #{item.LOW_PRICE}
                , #{item.TRADE_PRICE}
                , #{item.PREV_CLOSING_PRICE}
                , #{item.CHANGE}
                , #{item.CHANGE_PRICE}
                , #{item.CHANGE_RATE}
                , #{item.SIGNED_CHANGE_PRICE}
                , #{item.SIGNED_CHANGE_RATE}
                , #{item.TRADE_VOLUME}
                , #{item.ACC_TRADE_PRICE}
                , #{item.ACC_TRADE_PRICE_24H}
                , #{item.ACC_TRADE_VOLUME}
                , #{item.ACC_TRADE_VOLUME_24H}
                , #{item.HIGHEST_52_WEEK_PRICE}
                , TO_CHAR(TO_DATE(#{item.HIGHEST_52_WEEK_DATE}, 'YYYY-MM-DD'), 'YYYY-MM-DD')
                , #{item.LOWEST_52_WEEK_PRICE}
                , TO_CHAR(TO_DATE(#{item.LOWEST_52_WEEK_DATE}, 'YYYY-MM-DD'), 'YYYY-MM-DD')
                , TO_CHAR(TO_DATE(#{item.UPD_DT}, 'YYYY-MM-DD HH24:MI:SS'), 'YYYY-MM-DD HH24:MI:SS')
            FROM DUAL
        </foreach>
    </insert>

</mapper>