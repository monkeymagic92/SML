<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring.xsd http://www.springframework.org/schema/aop https://www.springframework.org/schema/aop/spring-aop.xsd"
>


    <!-- ########################### DB연동 정보 Start ########################### -->
    <!-- oracle 연동  -->
    <!--
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="oracle.jdbc.driver.OracleDriver"/>
        <property name="url" value="jdbc:oracle:thin:@localhost:1521:orcl"/>
        <property name="username" value="${db.orcl.username}"/>
        <property name="password" value="${db.orcl.password}"/>
    </bean>
    -->

    <!-- mysql / mariaDB 연동 -->

    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="net.sf.log4jdbc.sql.jdbcapi.DriverSpy"/>
        <property name="url" value="jdbc:log4jdbc:mysql://localhost:3306/SML?characterEncoding=UTF-8&amp;serverTimezone=UTC"/>
        <property name="username" value="${db.maria.username}"/>
        <property name="password" value="${db.maria.password}"/>
    </bean>


    <!-- mysql / mariaDB 서버연동(배포) -->
    <!--
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="com.mysql.cj.jdbc.Driver"/>
        <property name="url" value="jdbc:mysql://${db.server.url}/HiBaby?characterEncoding=UTF-8&amp;serverTimezone=UTC"/>
        <property name="username" value="${db.server.username}"/>
        <property name="password" value="${db.server.password}"/>
    </bean>
    -->
    <!-- ########################### DB연동 정보 End ########################### -->


    <!--  db관련 정보 properties에 넣기위함 용   /   경로 :  alt + ins - File -->
    <context:property-placeholder location="classpath:/config/config-properties" />


    <!-- 얘는 인터페이스의 @Mapper를 찾는 애 -->
    <mybatis-spring:scan base-package="com.sml.*"/> <!--  하단 탭에Namespace import 안해서 빨간줄 뜨는것  -->


    <!-- 'configLocation' - mybatis-alias.xml - mybatis alias 지정하는 파일
         'mapperLocations' - mapper - DB쿼리 작성  -->
    <bean id="sqlSessionFactory" class="com.sml.core.RefreshableSqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="classpath:/mybatis/mybatis-alias.xml"/>
        <!-- mapperLocations에서 classpath* 붙여줘도되고 안붙여줘도 되고 (Mapper.xml 파일을 읽는다)-->
        <property name="mapperLocations" value="classpath:/mapper/**/*Mapper.xml"/>
        <property name="interval" value="1000" />
    </bean>


    <bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate">
        <constructor-arg ref="sqlSessionFactory"></constructor-arg>
    </bean>


    <!-- AOP AspectJ Start -->
    <bean id="AopAdviceService" class="com.sml.aop.AopAdvice"/>
    <aop:config>
        <!-- AopAdviceService -->
        <aop:aspect ref="AopAdviceService">
            <aop:pointcut id="infoCallTrace" expression="execution(* com..*Controller.*(..))"/>
            <aop:before pointcut-ref="infoCallTrace" method="beforeMethodCall"/>
        </aop:aspect>
    </aop:config>
    <!-- AOP AspectJ End -->

</beans>