<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sml.quote.QuoteMapper">

    <!-- 테스트용 -->
    <select id="selectQuote" resultType="LinkedHashMap">
        SELECT
            MARKET
            , HIGH_PRICE
            , UPD_DT
        FROM T_COIN_TH_QUOTE_KRW
        WHERE MARKET = 'KRW-BTC'
    </select>

    <select id="selectThKRWUpdDt" resultType="String">
        SELECT
            DATE_FORMAT(UPD_DT, '%Y-%m-%d  /  %H:%i:%s')
        FROM T_COIN_TH_QUOTE_KRW
        WHERE MARKET = 'KRW-BTC'
    </select>

</mapper>